# Kanban MVP (Frontend)

Single-board Kanban web app (no persistence) with:
- 5 fixed columns (renamable)
- Cards with title + details
- Drag and drop between columns

## Run

```bash
npm install
npm run dev
```

Open `http://localhost:3000`.

## Tests

```bash
npm test
npm run test:e2e
```
